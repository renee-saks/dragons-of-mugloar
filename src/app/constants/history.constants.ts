import { MatDialogConfig } from '@angular/material/dialog';

export const historyDialogConfig: MatDialogConfig = {
  width: 'calc(100% - 30px)',
  height: 'calc(100% - 30px)',
  maxWidth: '100%',
  maxHeight: '100%',
};
